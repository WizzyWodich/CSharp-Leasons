using System;


namespace Leasons_10{

    class Program
    {
        static void Main(string[] args)
        {
            // Вызовы функции
            /*{
                Вызов функции а так же взаимодействия с ними(передача параметров)
                Все то что передаеться в функию называються аргументами функции args.
                Print("Это текст который я передал в функцию.");

                string name = "Этот текст был передан в функию через заранее созданую переменную.";
                Print(name);

            }*/ 

            //Практика передача в функцию масив для подсчета суммы.
            {
                // byte[] nums = {1,2,57,11,9};
                // byte result_1 = Sum(nums);
                // System.Console.WriteLine("Result: " + result_1);

                // byte[] nums_2 = {22,21,7,1,6};
                // byte result_2 = Sum(nums_2);
                // System.Console.WriteLine("Result: " + result_2);

            }
            
            Console.ReadLine();
        }
        //Урок 10 
            /*public static void Print(string word)
            {
                Console.WriteLine(word);
            }*/

        // Функция подсчета сумы в масиве.
            // public static byte Sum (byte[] digits){
            //     byte sum = 0;
            //     foreach (byte el in digits)
            //         sum += el;

            //     return sum;

            // }
        

    }
}


